{
  "_comment": "ApiBaseUrl is the game API origin. This value is identical to the fallback already compiled into Program.cs, so `dotnet run` behaves exactly as before. The file must exist at PUBLISH time to be registered in the Blazor boot manifest — the container entrypoint rewrites it from $API_BASE_URL so one image can serve any environment.",
  "ApiBaseUrl": "http://localhost:5210"
}
